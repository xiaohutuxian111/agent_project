[project]
name = "agent-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "stone", email = "2398391057@qq.com" }
]
requires-python = ">=3.14"
dependencies = [
    "json5>=0.15.0",
    "openai>=3.1.0",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
agent-project = "base_agent:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "base_agent"
